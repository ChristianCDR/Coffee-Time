services: 
  php:
    env_file:
      - ../back/.env.test

  database:
    env_file:
      - ../back/.env.test
    volumes:
      - ./postgres_test_data:/var/lib/postgresql/data
volumes:
  postgres_test_data: